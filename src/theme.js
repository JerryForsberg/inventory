// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#1976d2', // Blue color for primary theme
        },
        secondary: {
            main: '#dc004e', // Pink color for secondary theme
        },
    },
});

export default theme;
